const intialState = {
    identifier: null,
    token: null,
    isLogged: false,
};

export default (state = intialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}